<?xml version="1.0" encoding="UTF-8"?>
<OrdSet xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns="tfs"
        xsi:schemaLocation="tfs tfs.xsd"
        Version="36">
  <!--
  ==============================================================================
  TEST / SCHEMA WARNING

  Đây chỉ là mẫu tối giản của envelope runtime để kiểm tra XML/schema wiring.
  Không dùng file này làm order test, production payload hoặc ví dụ schema đầy đủ.
  `xsi:schemaLocation="tfs tfs.xsd"` phản ánh envelope source, nhưng repository
  không chứa canonical `tfs.xsd`; vì vậy location này không local-validate mẫu.
  Runtime cũng không ghi một envelope rỗng: khi OrderMSG rỗng, generator trả
  nội dung rỗng/không tạo artifact. File này chỉ là skeleton tài liệu.

  Runtime body placeholder: OrderMSG do UBOrderCreateFile và chuỗi stored procedure
  trong database cung cấp. Mẫu này cố ý không dựng FileHdr, Order, filename,
  sequence, checksum hoặc bất kỳ field nghiệp vụ nào.

  Source evidence:
  - DLLs/UBFFImport/COrder.cs:20-49
  - DLLs/UBFFImport/COrder.cs:86-121
  - MyPortfolioNew/VieFUND-Platform/src/SQLScript/000_4_CreateSP.sql:464617-464771
  ==============================================================================
  -->
</OrdSet>
